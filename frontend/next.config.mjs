/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,  // Keep React in strict mode for best practices
    experimental: {
      optimizeFonts: false, // Prevents Next.js from optimizing Google Fonts
    },
  };
  
  export default nextConfig;
  